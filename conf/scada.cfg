//
// ======= SCADA  definitions: =======

scadatitle = "SCADA config";

SCADA:
(
  { title = "SCADA";
    name = "-";
    desc  = "SCADA config";
    polling = 0; 	// (miLLi Seconds) polling interval 
    timeout = 0;	// (miCRo Seconds) timeout interval - how long to wait modbus response
    ip = "0.0.0.0";
    port = 0;
    attempts = 1;
    @include "scada_regs.cfg"
  }
);

